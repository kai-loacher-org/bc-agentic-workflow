name: Developer Test

on:
  workflow_call:
    inputs:
      issue_number:
        description: "Issue number"
        type: string
        required: true

jobs:
  develop:
    runs-on: agentic-developer
    timeout-minutes: 60
    steps:
      - name: Test step
        run: echo "Developer test works for issue ${{ inputs.issue_number }}"

name: Test Caller

on:
  workflow_dispatch:
    inputs:
      message:
        description: "Test message"
        type: string
        default: "hello"

jobs:
  call:
    uses: kai-loacher-org/bc-agentic-workflow/.github/workflows/test-reusable.yml@main
    with:
      test_input: ${{ inputs.message }}
    secrets: inherit
